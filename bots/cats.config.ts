export const images = {
  name: 'eyebleach',
  instances: 1,
  merge_logs: true,
  args:
    '--subreddits cats eyebleach --schedule "0 0 * * SUN" --insta username:password --tags cute eyebleach cats dogs',
};
